# <需求名称>

## 业务目标
<2-4 句描述完整业务目标和预期结果>

## 涉及服务与依赖顺序
1. <服务A>（先完成，因为 <服务B> 依赖其接口）
2. <服务B>（依赖 <服务A> 的接口）

## 数据库设计
<!-- 仅填写有 DB 变更的服务；若无 DB 变更，删除本节 -->

### [<服务名>]
| 表名 | 字段 | 类型 | 说明 |
|---|---|---|---|

## 接口定义
<!-- 提供方服务写完整定义；调用方写"依赖 [xxx] 接口"引用，不重复定义字段 -->

### [<提供方服务>] <METHOD> <路径>
- 描述：
- 请求：`{ <字段>: <类型> }`
- 响应：`{ code, msg, data: { <字段>: <类型> } }`
- 规则：

### [<调用方服务>] 依赖的接口
- 调用 `[<提供方服务>] <METHOD> <路径>`（见上方定义）

## 代码地图
<!-- 受影响的关键文件/类/方法，按服务分小标题 -->

### [<服务名>]
- `<文件路径>:<方法名>()` — <职责描述>

## 任务
<!-- 格式：T-<序号> · [<服务名>] · P0/P1/P2 · 状态 — 描述（file:method） -->
<!-- 序号全文档唯一递增；状态：待开始 → 进行中 → ✅ / 已取消（附原因） -->

## 验收
<!-- 格式：A-<序号> · [<服务名>|E2E] · <接口/场景> · 状态 — 可执行断言 -->
<!-- [E2E] 表示跨服务集成验收；序号全文档唯一递增 -->
