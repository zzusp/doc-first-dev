# <模块名> 技术方案

> **文档目的**：记录 <模块名> 的需求、架构方案、代码结构、任务进度与验收情况。
> 本文档始终反映**当前状态**，不记录变更历史。
>
> **关联文档**：
> - `docs/decisions/log.md` — 开发决策记录（via `/whylog-record`）

---

## 一、需求边界

### 1.1 业务背景

<!-- 简述该模块解决什么问题、服务于哪类用户或业务场景 -->

### 1.2 核心需求

<!-- 用编号列表列出本期需要实现的功能，每条明确输入、输出和预期行为 -->

1. **功能 A**：...
2. **功能 B**：...

### 1.3 明确排除范围

<!-- 本期明确不做的功能，防止范围蔓延 -->

- ...

---

## 二、数据库设计

### 2.1 表清单

| 表名 | 用途 | 关键字段 |
|---|---|---|
| `<table_name>` | ... | `id`, `...` |

### 2.2 字段说明

<!-- 对关键字段的语义、约束、枚举值进行说明 -->

```
<field_name>  — <语义说明>
<field_name>  — <语义说明>（枚举：0=xxx, 1=xxx）
```

### 2.3 JSONB / 嵌套结构（如有）

<!-- 描述 JSON 字段内部结构 -->

### 2.4 约束规则

<!-- 唯一键、外键、业务唯一性约束等 -->

---

## 三、接口规范

### 3.1 接口列表

| Method | Path | 功能 | 权限 |
|---|---|---|---|
| POST | `/api/<resource>/list` | 分页查询 | 登录用户 |
| POST | `/api/<resource>/save` | 新增/保存 | 登录用户 |
| DELETE | `/api/<resource>/{id}` | 删除 | 编辑人 |

### 3.2 关键行为规则

<!-- 以"规则："形式明确列出业务规则、权限规则、幂等性要求等 -->

- 规则：...
- 规则：...

---

## 四、架构与设计规则

### 4.1 模块关系

<!-- 当前模块与其他模块的依赖、调用、数据流向 -->

### 4.2 数据流

```
请求 → Controller（参数校验）→ Service（业务逻辑）→ Mapper/Repository（数据访问）→ DB
```

### 4.3 设计规则

<!-- 以"规则："形式明确列出，开发时必须遵守 -->

- 规则：...
- 规则：...

---

## 五、代码地图

### 5.1 关键文件

| 文件路径 | 职责 |
|---|---|
| `src/.../XxxController.<ext>` | 接口入口，参数校验 |
| `src/.../XxxService.<ext>` | 业务逻辑接口定义 |
| `src/.../XxxServiceImpl.<ext>` | 业务逻辑实现 |
| `src/.../XxxMapper.<ext>` | 数据访问层 |

### 5.2 关键方法

| 方法签名 | 做什么 | 维持哪些行为 |
|---|---|---|
| `XxxService.list(query)` | 分页查询，支持条件筛选 | 返回 `{total, records}` 结构 |

---

## 六、任务状态

| 编号 | 描述（文件 + 方法/节点） | 优先级 | 状态 |
|---|---|---|---|
| T-01 | 在 `XxxMapper.xml` `selectPageList` 追加 xxx 条件；更新 `XxxMapper.java` 方法签名 | P0 | 待开始 |

> P0 = 阻塞，P1 = 重要，P2 = 可选。取消时保留行并注明原因。

---

## 七、验收项

### Phase 0 — 准备

> 参考项目根目录 `CLAUDE.md` 中的"启动与认证"章节。

1. 启动应用
2. 获取认证信息，保存请求头
3. 确认测试数据：
   - 测试账号：`<account>`
   - 测试数据 ID：`<id>`

### Phase 1 — 功能验收

| 编号 | 名称 | 关联任务 | 断言（具体请求 + 预期响应） | 状态 |
|---|---|---|---|---|
| A-01 | <验收项名称> | T-01 | `POST /api/xxx/list` 传 `field=value`，返回 `total>0`，所有结果满足条件 | 待验证 |

### Phase 2 — 非功能验收

| 编号 | 名称 | 关联任务 | 断言 | 状态 |
|---|---|---|---|---|
| A-NF-01 | 构建通过 | — | 运行构建命令无报错 | 待验证 |
| A-NF-02 | 应用启动 | — | 启动后健康检查接口返回 200 | 待验证 |

---

## 八、附录

### 8.1 枚举值

| 枚举名 | 值 | 含义 |
|---|---|---|
| `StatusEnum.ACTIVE` | `1` | 启用 |
| `StatusEnum.INACTIVE` | `0` | 禁用 |

### 8.2 固定测试数据

| 数据项 | 值 | 说明 |
|---|---|---|
| 测试账号 | `test@example.com` | 标准测试账号 |
| 测试数据 ID | `<uuid>` | 用于验收 A-01 |
