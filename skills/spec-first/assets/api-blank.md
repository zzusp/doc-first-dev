# {{模块名}} 模块接口文档

> **模块说明**：{{模块功能描述，说明该模块管理的资源及支持的操作}}
>
> **Base Path**：所有接口均以 `/{{basePath}}` 为前缀。
>
> **认证**：{{描述认证方式，如：所有请求需在 Header 中携带 `Authorization: Bearer <token>`}}

---

## 认证 Header

<!-- 按项目实际认证方案填写；不需要则删除本节 -->

| Header 名 | 必填 | 说明 |
|---|---|---|
| `Authorization` | 是 | {{认证令牌说明}} |
| `{{headerName}}` | {{是/否}} | {{说明}} |

---

## 统一响应格式

<!-- 按项目实际响应结构填写；不需要则删除本节 -->

所有接口均返回统一的响应结构。

```json
{
  "{{字段名}}": "{{说明}}",
  "{{字段名}}": "{{说明}}"
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `{{字段名}}` | {{类型}} | {{说明}} |

---

## 枚举值说明

<!-- 如无枚举值可删除本节；每组枚举单独建一个小节 -->

### {{enumName}} — {{枚举用途描述}}

| 枚举值 | 说明 |
|---|---|
| `ENUM_VALUE_1` | {{说明}} |
| `ENUM_VALUE_2` | {{说明}} |

---

## 接口列表

<!-- 
  按实际接口数量复制下方的「接口块」，每个接口一节。
  分组方式（按资源、按功能域等）根据项目自行决定。
  示例中提供了两种常见请求形式（Query 参数 / 请求体），按需保留其一。
-->

---

### 1. {{接口功能名}}

**`{{METHOD}} /{{path}}`**

{{接口详细说明，描述业务含义、前置条件、关键约束等。}}

#### 请求参数

<!-- Query 参数形式（GET/DELETE 常用）—— 不需要则删除 -->

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `{{param}}` | string | **是** | {{说明}} |
| `{{param}}` | string | 否 | {{说明}} |

<!-- 请求体形式（POST/PUT/PATCH 常用）—— 不需要则删除 -->

```json
{
  "{{field}}": "{{value}}",
  "{{field}}": "{{value}}"
}
```

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `{{field}}` | string | **是** | {{说明}} |
| `{{field}}` | string | 否 | {{说明}} |

#### 响应

```json
{
  "{{field}}": "{{value}}"
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `{{field}}` | {{类型}} | {{说明}} |

#### 错误情况

<!-- 只列出该接口特有的错误；通用错误（认证失败、系统异常等）已在统一响应格式中说明 -->

| 错误码 | 说明 | 触发条件 |
|---|---|---|
| `{{code}}` | {{message}} | {{触发条件}} |

---

### 2. {{接口功能名}}

<!-- 复制上方接口块继续填写 -->

---

## 接口汇总

| # | 方法 | 路径 | 说明 |
|---|---|---|---|
| 1 | {{METHOD}} | `/{{path}}` | {{说明}} |
| 2 | {{METHOD}} | `/{{path}}` | {{说明}} |
